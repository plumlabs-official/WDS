---
name: creating-subagent
description: Creates custom subagents for Claude Code and Cursor. Use when user says "서브에이전트 만들어줘", "subagent 생성", "에이전트 만들어줘", or needs isolated context/tool restrictions. For knowledge injection into current context, use creating-skills instead.
---

# Custom Subagent 생성

Custom Subagent를 생성하여 특정 작업에 특화된 AI 어시스턴트를 만듭니다.

---

## 🚦 Step 0: Skill vs Subagent 결정 (필수)

**⚠️ 생성 전 반드시 확인:** 사용자 요청이 Subagent에 적합한지 판단한다.

### Subagent가 적합한 경우 ✅

- **컨텍스트 분리** 필요 (긴 탐색/연구 결과가 메인 대화 오염 방지)
- **도구 접근 제한** 필요 (read-only, Bash만 허용 등)
- **병렬 실행** 필요 (여러 작업 동시 수행)
- 완료된 작업의 **독립 검증**

### Skill이 적합한 경우 → `creating-skills` 스킬로 안내 ❌

- Claude에게 **전문 지식/가이드라인** 추가 (예: "우리 팀 PR 표준으로 리뷰해줘")
- **현재 대화 컨텍스트**에서 실행되어야 함
- Claude가 **자동으로** 관련 상황에서 적용해야 함
- 단일 목적의 빠른 작업 (changelog 생성, 커밋 메시지, 포맷팅)

### AI 추론 후 제안 (애매한 경우)

사용자 요청을 분석하여 AI가 먼저 판단하고 제안한다:

```
🤔 요청을 분석해보니 **{Skill/Subagent}**가 더 적합해 보입니다.

**이유:**
- {핵심 판단 근거 1-2개}

**예시:** {유사한 기존 Skill/Subagent 언급}

이 방향으로 진행할까요? 아니면 다른 방식을 원하시나요?
```

**Skill이 적합하다고 판단되면:**
- 위 형식으로 Skill 추천 이유를 설명
- 사용자 동의 시 `creating-skills` 스킬로 전환

---

## 📚 공식 문서 참고 (Anthropic)

> **Skills vs. subagents**: Skills add knowledge to the current conversation.
> Subagents run in a separate context with their own tools.
> Use Skills for guidance and standards; use subagents when you need isolation or different tool access.
> — [Claude Code Docs: Agent Skills](https://code.claude.com/docs/en/skills)

> **Use subagents when:**
> - The task produces verbose output you don't need in your main context
> - You want to enforce specific tool restrictions or permissions
> - The work is self-contained and can return a summary
> — [Claude Code Docs: Sub-agents](https://code.claude.com/docs/en/sub-agents)

---

## AI 수행 절차

**상세 절차:** `references/1_system-prompt-guide.md`

> **⚠️ 전제조건**: Step 0에서 Subagent가 적합하다고 결정된 후에만 진행

### To-Do 생성

스킬 시작 시 아래 To-Do를 생성하여 진행 상황을 추적한다:

```
□ Step 0: Skill vs Subagent 결정 (필수) ← 완료 확인
□ Step 1: 역할 인터뷰 (Role, Goal, Trigger 수집)
□ Step 2: Frontmatter 확정 + 파일 생성
□ Step 3: System Prompt 인터뷰 (Input, Output, 가이드)
□ Step 4: 초안 작성 (Role, Workflow, Output Format)
□ Step 5: 전문가 리뷰 시뮬레이션
□ Step 6: 최종 업데이트 + 사용자 확인
```

---

### Step 1: 역할 인터뷰

- **인풋**: 사용자의 Subagent 생성 요청
- **아웃풋**: Role, Goal, Trigger 3가지 정보 수집

사용자에게 다음 질문을 한다:

1. **Role (역할)**: "어떤 전문가인가요?"
2. **Goal (목표)**: "수행할 핵심 과업은 무엇인가요?"
3. **Trigger (트리거)**: "언제 호출되어야 하나요?"

---

### Step 2: Frontmatter 확정 + 파일 생성

- **인풋**: Step 1에서 수집한 Role, Goal, Trigger
- **아웃풋**: 사용자 승인된 Frontmatter + 생성된 파일 경로

Step 1 결과를 바탕으로 Frontmatter를 작성하고 사용자에게 제시한다.

**포맷:**

```yaml
---
name: {kebab-case-name}
description: |
  {역할}. {역할 설명}.
  Use {when/immediately/proactively} {상황}.
  한국어 트리거: "{키워드1}", "{키워드2}", "{키워드3}".
model: inherit
---
```

---

⏸️ **STOP: 사용자 승인 대기**
1. 다음 응답 중 하나를 받을 때까지 파일 생성으로 진행하지 않는다: "승인", "진행해", "OK", "좋아"
2. 사용자가 수정 요청 시 → Frontmatter 수정 후 다시 승인 요청.
3. **승인 받으면 같은 턴에서** 파일 생성 후 Step 3 질문을 시작한다.

---

**파일 생성:**

1. **파일 생성**: `.claude/agents/{name}.md`
2. **내용**: Frontmatter + 빈 템플릿 (`references/2_system-prompt-template.md` 구조)


| 범위   | 경로                  | 설명           |
| ---- | ------------------- | ------------ |
| 프로젝트 | `.claude/agents/`   | 팀 공유, Git 추적 |
| 개인   | `~/.claude/agents/` | 모든 프로젝트에서 사용 |


---

### Step 3: System Prompt 인터뷰

**📖 `references/1_system-prompt-guide.md` Part A를 수행한다.**

- **인풋**: Step 2에서 생성된 파일 경로
- **아웃풋**: 사용자의 Input/Output/가이드 답변 수집

사용자에게 다음 3가지를 질문한다:

1. **Input**: "이 에이전트가 작업을 시작하려면 어떤 정보가 필요한가요?"
2. **Output**: "최종 결과물은 어떤 형태로 받고 싶으세요?"
3. **참고 가이드**: "참고할 가이드가 있나요? (`docs/guides/`)"

---

⏸️ **STOP: 사용자 답변 대기**

3가지 질문에 대한 사용자 답변을 받을 때까지 Step 4로 진행하지 않는다.

---

### Step 4: 초안 작성

**📖 `references/1_system-prompt-guide.md` Part B를 수행한다.**

- **인풋**: Step 2 파일 + Step 3 인터뷰 결과
- **아웃풋**: 빈 섹션이 채워진 System Prompt 초안

Step 2에서 생성한 파일의 빈 섹션을 채운다:

- `## Role` - 역할 정의 + 핵심 원칙 3개 + 참고 가이드
- `## Workflow` - Step-by-Step 작업 절차
- `## Output Format` - 구체적인 출력 형식

---

### Step 5: 전문가 리뷰 시뮬레이션

**📖 `references/1_system-prompt-guide.md` Part C를 수행한다.**

- **인풋**: Step 4에서 작성된 초안
- **아웃풋**: 전문가 리뷰 결과 + 개선 방향 + 수정된 초안

두 명의 가상 전문가가 검토하는 과정을 시뮬레이션한다:

1. **Anthropic 수석 프롬프트 엔지니어**: 구조적 결함, 모호한 지시, 환각 가능성 지적
2. **도메인 전문가**: 비즈니스 목표 달성 여부, 실무 적합성 지적

리뷰 결과를 사용자에게 보여주고, 리뷰를 반영하여 초안을 수정한다.

---

### Step 6: 최종 업데이트 + 확인

- **인풋**: Step 5에서 수정된 초안
- **아웃풋**: 최종 저장된 파일 경로 + 사용자 확인 요청

1. 리뷰 반영하여 `.claude/agents/{name}.md` 업데이트
2. 사용자에게 확인 요청: "리뷰 반영하여 저장했습니다. 파일 확인 후 수정 요청 주세요."

**🔗 Cursor 동시 지원 (심볼릭 링크, 1회 실행):**

```bash
ln -s ../.claude/agents .cursor/agents
```

---

## 📢 Subagent 호출 방법

> **⚠️ Command 등록 불필요**: Subagent는 Skill과 다르게 `/start-xxx` Command가 필요 없습니다.

### 호출 방법 (공식 문서)

**1. 자연어로 명시적 호출:**
```
ux-designer 에이전트 사용해서 이 PRD 분석해줘
```

**2. 자동 위임 (Automatic Delegation):**
- Claude가 subagent의 `description`을 보고 적절한 작업에 자동 위임
- 예: "UX 설계해줘" → description에 "UX 설계" 트리거가 있으면 자동 선택

### 흔한 실수

- ❌ Subagent에 Command 등록하려고 함 → 불필요
- ❌ `/start-ux-design` 같은 슬래시 명령어 기대 → Skill 방식
- ✅ "ux-designer 에이전트 사용해서..." 자연어 호출

---

## ⚠️ 제약사항 및 Anti-patterns

### Subagent 기술적 제약 (공식 문서)

- **중첩 불가**: Subagent는 다른 Subagent를 호출할 수 없음 → 체이닝이 필요하면 메인 대화에서 순차 호출
- **MCP 도구 제한**: Background subagent에서는 MCP 도구 사용 불가
- **Skill 미상속**: Subagent는 메인 대화의 Skill을 자동 상속하지 않음 → `skills:` 필드로 명시 필요
- **권한 제약**: Background subagent는 권한 프롬프트 자동 거부됨

### 흔한 실수들

- **수십 개의 범용 subagent**: Agent가 언제 위임할지 모름
- **모호한 description**: "Use for general tasks" → 신호 없음
- **2000단어 이상 프롬프트**: 느리고 유지보수 어려움
- **단순 작업에 subagent 사용**: Skill이 더 적합
- **Step 건너뛰기**: 품질 저하, 사용자 불만

---

## 검증 체크리스트

**필수:**

- `.claude/agents/{name}.md` 생성
- `description`: 역할 + 구체적 트리거 조건
- System Prompt: 구조화된 Step-by-Step
- 모든 STOP 체크포인트에서 사용자 승인 획득

**권장:**

- 300줄 이내
- tools: 최소 필요 권한만 지정
- 심볼릭 링크로 Cursor 지원

---

## 📚 References

- `references/1_system-prompt-guide.md` - System Prompt 설계 상세 (Part A/B/C)
- `references/2_system-prompt-template.md` - 표준 템플릿
- `references/examples.md` - 실제 예시 모음

**외부 문서:**

- [Claude Code Subagents](https://code.claude.com/docs/en/sub-agents)
- [Cursor Subagents](https://cursor.com/docs/agent/subagents)
