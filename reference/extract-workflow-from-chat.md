# @extract-workflow - 채팅 히스토리를 재사용 가능한 워크플로우로 추출

> **이 메뉴는 성공적인 AI 협업 경험을 재사용 가능한 워크플로우 문서로 변환합니다.**

---

## 🎯 목적

- 휘발되는 성공적인 AI 협업 경험을 구조화된 문서로 포착
- 반복 작업 시 처음부터 다시 프롬프팅하는 비효율 제거
- 명확한 Input/Output 구조로 결과물 품질 표준화

---

## 📋 성공의 정의

1. **Output**: 각 Step별 Input/Output이 명확히 정의된 워크플로우 문서 (`prompts/mcdonalds/{menu-name}.md`)
2. **작동**: 원본 채팅을 모르는 사람(또는 AI)도 이 문서만으로 동일한 결과를 재현 가능

---

## 🔄 AI 수행 절차

**아래 절차에 맞춰 커서 to-dos 생성하고, to-dos 순서대로 절차를 하나씩 진행합니다.**

### Step 0: 스코프 확정 (사용자 확인 필수)

- **인풋**: 사용자에게 다음을 질문합니다.

  ```
  🎯 워크플로우 추출을 시작합니다.

  1. 이 채팅에서 **어떤 작업**을 워크플로우로 만들고 싶으신가요?
  2. **집중할 구간**이나 **제외할 시행착오**가 있나요?
  3. 희망하는 **메뉴 이름**이 있나요? (없으면 제가 제안합니다)
  ```

- **아웃풋**: 추출 목표, 포커스 구간, 메뉴 이름 확정.

---

### Step 1: 분석 및 정제 (AI 자동 실행)

- **인풋**: Step 0에서 확정된 스코프 + 사용자에게 채팅 히스토리 전체 첨부 요청
- **작업**:
  - 대화의 시작(목표 설정)과 끝(최종 결과)을 파악.
  - **노이즈 판별 기준**:
    - ❌ 오류 발생 후 롤백한 시도
    - ❌ "아, 잠깐", "다시 해볼게" 등 방향 전환
    - ❌ 주제와 무관한 잡담
  - 노이즈는 제거하거나 "주의사항"으로 분류.
- **아웃풋**: 정제된 핵심 흐름 요약. 다음 형식으로 출력:

  ```
  📊 채팅 분석 결과:

  ✅ 핵심 흐름:
  1. [첫 번째 주요 액션]
  2. [두 번째 주요 액션]
  ...

  ❌ 제외된 노이즈:
  - [시행착오 1]: [이유]

  ⚠️ 주의사항으로 전환:
  - [실수했던 부분] → "~하지 말 것" 으로 문서화 예정
  ```

---

### Step 2: 구조화 및 변수 추출 (AI 제안 → 사용자 확인)

- **인풋**: Step 1의 핵심 흐름 요약.
- **작업**:
  - 정제된 흐름을 논리적 단계(Step N)로 분할.
  - 고정값과 변하는 값을 구분하여 변수(`{{...}}`) 추출.
- **아웃풋**: 구조화 초안 + 변수 목록. 다음 형식으로 출력 후 **사용자 승인 요청**:

  ```
  📐 구조화 초안:

  | Step | 이름 | 설명 |
  |------|------|------|
  | 1 | [단계명] | [한 줄 설명] |
  | 2 | [단계명] | [한 줄 설명] |
  ...

  🔑 식별된 변수:
  - {{VAR_1}}: [설명] (예시: "React")
  - {{VAR_2}}: [설명] (예시: "Button")

  이 구조가 맞나요? 수정할 부분 있으면 알려주세요.
  ```

- **분기**: 승인 → Step 3. 수정 요청 → Step 2 반복.

---

### Step 3: 상세 명세 작성 (AI 자동 실행)

- **인풋**: 승인된 구조 + 원본 채팅에서 성공했던 프롬프트 패턴.
- **작업**: 각 Step별로 상세 명세 작성.
- **아웃풋**: 다음 형식 준수:

  ```markdown
  ### Step N: [단계명]

  - **인풋**: [이 단계 시작에 필요한 정보]
  - **아웃풋**: [이 단계 완료 시 생성되는 결과물]
  - **AI 프롬프트 (참고용)**:
  ```

  [채팅에서 효과적이었던 프롬프트 패턴]

  ```

  ```

---

### Step 4: 셀프 리뷰 (AI 자동 실행)

- **인풋**: Step 3에서 작성된 상세 명세.
- **셀프 리뷰 프롬프트**:

  ```
  이 워크플로우를 처음 보는 AI가 실행한다고 가정하고, 다음을 검증하세요:

  1. Step 1의 Output만으로 Step 2를 시작할 수 있는가?
  2. 모든 변수({{...}})가 어느 시점에 입력받는지 명확한가?
  3. 모호하거나 해석이 필요한 지시가 있는가?

  문제가 있으면 수정하세요.
  ```

- **아웃풋**: 검증 완료된 상세 명세 또는 수정된 버전.

---

### Step 5: 문서 생성 (AI 자동 실행)

- **인풋**: Step 4에서 검증된 명세.
- **작업**: 다음 템플릿 기반으로 마크다운 파일 생성.
- **아웃풋**: 워크플로우 문서 (`prompts/mcdonalds/{menu-name}.md`)

**📄 결과물 템플릿:**

```markdown
# @{menu-name} - {한 줄 설명}

> **이 메뉴는 {목적}을 자동화합니다.**

---

## 🎯 목적

- {이 워크플로우가 달성하는 것}

---

## 📋 성공의 정의

1. **Output**: {최종 결과물}
2. **작동**: {완료 후 동작}

---

## 🔑 변수 정의

| 변수      | 설명 | 예시 |
| --------- | ---- | ---- |
| {{VAR_1}} | ...  | ...  |

---

## 🔄 AI 수행 절차

{Step 3-4에서 작성된 상세 명세}

---

## ⚠️ 주의사항 (Anti-patterns)

- {Step 1에서 식별된 시행착오 기반}

---

## 💡 사용 예시

{이 워크플로우를 호출하는 방법}
```

---

### Step 6: 사용자 리뷰 및 승인 (사용자 확인 필수)

- **인풋**: 생성된 문서.
- **아웃풋**:

  ```
  ✅ 워크플로우 문서 초안입니다.

  📄 파일: prompts/mcdonalds/{menu-name}.md

  검토 부탁드립니다. 수정이 필요하면 말씀해주세요.
  ```

- **분기**: 승인 → Step 7. 수정 요청 → Step 3 또는 Step 5로 돌아감.

---

### Step 7: 최종 저장 및 사용 가이드

- **인풋**: 사용자 최종 승인.
- **아웃풋**:

  ```
  ✅ 워크플로우 추출 완료!

  📄 저장됨: prompts/mcdonalds/{menu-name}.md

  🚀 사용 방법:
  @mcdonalds/{menu-name} 또는 파일을 멘션하여 실행하세요.

  💡 사용 예시:
  "이번에 {예시 상황}을 진행하려고 해. @mcdonalds/{menu-name} 실행해줘."
  ```

---

## ⚠️ AI 핵심 원칙

### ❌ 절대 금지

| 금지 사항                      | 이유                                                |
| ------------------------------ | --------------------------------------------------- |
| **문맥 없는 Raw Text 복사**    | 정제 없는 복붙은 재사용성 해침 (코드 템플릿은 예외) |
| **변수 하드코딩**              | `"Next.js"` ❌ → `"{{FRAMEWORK}}"` ✅               |
| **추상적 지시어**              | `"적절히"`, `"알아서"` ❌ → 구체적 기준 필수        |
| **시행착오를 절차에 포함**     | 오류 수정 과정은 `주의사항`으로 분류                |
| **사용자 확인 없이 구조 확정** | Step 2는 반드시 승인 후 진행                        |

### ✅ 반드시 실행

| 필수 사항                    | 이유                                               |
| ---------------------------- | -------------------------------------------------- |
| **스코프 확정 선행**         | 같은 채팅도 관점에 따라 다른 워크플로우 가능       |
| **프롬프트 패턴 보존**       | 결과물 생성에 기여한 핵심 프롬프트는 코드 블록으로 |
| **Input/Output 연결성 검증** | Step N Output → Step N+1 Input 가능한지 확인       |
| **간결성 유지**              | 각 Step은 핵심만 요약                              |
| **사용 예시 제공**           | 완성된 워크플로우 호출 방법 포함                   |

---

## 📚 참고 자료

- [에이전트 메뉴 생성 레시피](./start-new-recipe.md)
- [기능 개발 워크플로우 예시](../start_feature/1_get_context.md)
